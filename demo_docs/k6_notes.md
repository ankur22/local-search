# k6 v1.3.0 release notes

k6 v1.3.0 adds a new threshold abort feature so tests fail fast when an SLO is
breached. It also improves the Prometheus remote write output and adds better
support for health-check probes in synthetic monitoring.
